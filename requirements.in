# Core runtime（PyTorch GPU版は公式の cu126 インデックス経由で取得）
torch==2.7.1 ; platform_system == "Linux"
torchvision==0.22.1 ; platform_system == "Linux"
torchaudio==2.7.1 ; platform_system == "Linux"

# Embedding & search
open_clip_torch
numpy<2
# FAISS は Makefile 側で --no-deps 指定の個別インストール運用

# Video preprocessing & scoring
scenedetect[opencv]
opencv-python-headless==4.8.1.78
scikit-image
tifffile==2024.8.30
lpips

# Orchestration & logging
hydra-core==1.3.2
omegaconf==2.3.0
mlflow-skinny

# Dev tooling
pip-tools
