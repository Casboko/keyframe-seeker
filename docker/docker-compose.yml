version: "3.9"

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ..:/workspace
    environment:
      PYTORCH_INDEX_URL: https://download.pytorch.org/whl/cu126
    runtime: nvidia
